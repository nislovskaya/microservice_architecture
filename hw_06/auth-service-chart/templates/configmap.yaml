apiVersion: v1
kind: ConfigMap
metadata:
  name: auth-app-config
  namespace: m
data:
  SERVER_PORT: "8080"
  DB_HOST: "auth-postgresql"
  DB_PORT: "5432"
  DB_NAME: "users"
  JWT_SECRET: "secret"

---

apiVersion: v1
kind: ConfigMap
metadata:
  name: jwt-config
  namespace: m
data:
  SERVER_PORT: "8081"
  JWT_SECRET: "secret"

---

apiVersion: v1
kind: ConfigMap
metadata:
  name: user-app-config
  namespace: m
data:
  SERVER_PORT: "8082"
  DB_HOST: "user-postgresql"
  DB_PORT: "5432"
  DB_NAME: "users"
